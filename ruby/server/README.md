# Ruby reference server

The future reference server will wrap the pure Ruby engine with a deployable,
API-only service. The intended responsibilities include PostgreSQL persistence,
atomic command ordering, idempotency, authentication boundaries, projections,
HTTP APIs, real-time subscriptions, audit access, health, metrics, and logging.

No server framework has been generated yet. Service work begins only after the
first engine behavior is specified and implemented.
